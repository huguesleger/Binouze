{% extends ":admin:index.html.twig" %}

{% block body %}
    <div class="header">
        {%for actu in actu%}
            <div class="container">
                <div class="row">
                    <div class="actus">
                        <div class="col-sm-6 col-md-3 col-md-offset-2">
                            <img src="{{asset('./images/') ~ actu.images~('.png')}}" alt="">
                        </div>
                        <div class="col-sm-6 col-md-4">
                            <div class="title-actu">
                                <h3>{{actu.titre}}</h3>   
                            </div>

                            <div class="txt-actu">
                                <p>{{actu.news}}
                                    <br> <a href="#"><span class="suite">lire la suite</span></a>
                                </p>
                            </div>
                            <table class="infos table">
                                <thead>
                                    <tr>
                                        <th class="separation-auteur"> Publié le {{actu.date}}</th>
                                        <th> Par {{actu.auteur}}</th>
                                    </tr>
                                </thead>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
            <div class="separation-actus">
            </div>
        {% endfor %}
    </div>
{% endblock %}